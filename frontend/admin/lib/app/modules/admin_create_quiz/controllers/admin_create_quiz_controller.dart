import 'package:get/get.dart';

// Controller stub for the Create Quiz flow. Extend this controller with
// form state, question management, validation and API calls required to
// persist quizzes. Lifecycle hooks (onInit/onReady/onClose) are provided
// and can be used to initialize or dispose resources.
class AdminCreateQuizController extends GetxController {
  final count = 0.obs; // example reactive property; replace as needed

  @override
  void onInit() {
    super.onInit();
  }

  @override
  void onReady() {
    super.onReady();
  }

  @override
  void onClose() {
    super.onClose();
  }

  void increment() => count.value++;
}
