import 'package:get/get.dart';

// Controller for the Create Subject page. This is a scaffolded controller
// intended to be extended with form state, validation, and API calls to
// persist subject data. Lifecycle hooks are available for initialization
// and cleanup.
class AdminCreateSubjectController extends GetxController {
  final count = 0.obs;

  @override
  void onInit() {
    super.onInit();
  }

  @override
  void onReady() {
    super.onReady();
  }

  @override
  void onClose() {
    super.onClose();
  }

  void increment() => count.value++;
}
